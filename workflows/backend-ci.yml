# name: Backend CI

# on:
#   push:
#     branches:
#       - main
#       - master
#       - develop
#   pull_request:
#     branches:
#       - main
#       - master
#       - develop
#   workflow_dispatch:

# jobs:
#   test:
#     name: Lint and test backend (Node ${{ matrix.node-version }})
#     runs-on: ubuntu-latest
#     strategy:
#       fail-fast: false
#       matrix:
#         node-version: [18, 20]

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ matrix.node-version }}
#           cache: npm

#       - name: Install dependencies
#         run: npm ci

#       - name: Run lint
#         run: npm run lint

#       - name: Run tests
#         env:
#           NODE_ENV: test
#           JWT_SECRET: test-jwt-secret
#           JWT_REFRESH_SECRET: test-jwt-refresh-secret
#         run: npm run test:ci

#       - name: Upload JUnit report
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: backend-junit-report-node-${{ matrix.node-version }}
#           path: reports/junit
#           if-no-files-found: ignore

#       - name: Upload coverage
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: backend-coverage-node-${{ matrix.node-version }}
#           path: coverage
#           if-no-files-found: ignore
